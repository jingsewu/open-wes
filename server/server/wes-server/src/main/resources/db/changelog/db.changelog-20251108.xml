<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="create_table_a_api_key" author="Kinser">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="a_api_key"/>
            </not>
        </preConditions>
        <comment>create table a_api_key</comment>
        <sql>
            CREATE TABLE a_api_key
            (
                `id`           BIGINT                                                       NOT NULL PRIMARY KEY,
                `api_key_name` VARCHAR(255)                                                 NOT NULL,
                `api_key`      VARCHAR(255),
                `create_time`  bigint                                                       NOT NULL DEFAULT 0 COMMENT 'Creation time',
                `create_user`  varchar(60) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL DEFAULT '' COMMENT 'Create user',
                `update_time`  bigint                                                       NOT NULL DEFAULT 0 COMMENT 'Update time',
                `update_user`  varchar(60) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL DEFAULT '' COMMENT 'Update user'
            );

            -- Create unique indexes
            CREATE UNIQUE INDEX uk_api_key ON a_api_key (api_key);
            CREATE UNIQUE INDEX uk_api_key_name ON a_api_key (api_key_name);
        </sql>
    </changeSet>


</databaseChangeLog>
